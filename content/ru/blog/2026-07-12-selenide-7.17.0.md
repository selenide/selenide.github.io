---
slug: "selenide-7.17.0"
date: 2026-07-12
title: "Вышла Selenide 7.17.0"
description: ""
category:
headerText: "Роли решают всё"
tags: []
---

Привет!

Пока весь мир спорит, кто там нападающий, а кто вратарь, и последние восемь команд на Мундиале 
выясняют, кому сегодня вечером ехать домой, — у нас тут тоже разборки с ролями.

Встречайте [Selenide 7.17.0](https://github.com/selenide/selenide/milestone/242?closed=1)!

> Судья свистит. Роли распределены. Оффсайда не будет.  
> ARIA role="button" не должен получать красную карточку только за то, что он на самом деле `<div>`.

* [Локатор `byRole` — поиск элемента по ARIA-роли](#by-role-locator)
* [Ещё MCP-тулзы для вашего ИИ-раба](#more-mcp-tools)
* [Скачивание нескольких файлов разом](#download-multiple-files)
* [SelenideAppiumElement теперь честно возвращает SelenideAppiumElement](#appium-element-return-type)
* [`Selenide.cookies()` — новый API для печенек](#cookies-api)
* [`element()`/`elements()` для котлин-разработчиков в Appium](#element-aliases-kotlin)
* [Обновили Selenium с 4.44.0 на 4.45.0](#update-to-selenium-4.45.0)
* [Обновили Selenium с 4.45.0 на 4.46.0](#update-to-selenium-4.46.0)

<br>

### Локатор `byRole` — поиск элемента по ARIA-роли {#by-role-locator}

Знаете, в футболе есть роли: вратарь, защитник, нападающий. В вебе тоже есть роли: `button`, `link`, `checkbox`, 
`navigation`. И до сих пор Selenide как-то умудрялся жить без нормального способа искать элементы по этим ролям.

Непорядок. Исправили. Подсмотрели, как это сделано в Playwright (ну а что, хорошие идеи надо красть, тестировщики 
не гордые):

```java
$(byRole("button")).click();
$(byRole("button", "Купить билет на трибуну")).click();
$(byRole("checkbox", "Согласен с офсайдными правилами", match().exact())).click();
```

Причём роль ищется не только по явному атрибуту `role="button"`, но и по неявной роли тэга — если у вас честный 
`<button>` или `<a href="...">`, Selenide тоже его найдёт, без всяких `role=""`. Список поддерживаемых ролей 
приличный: `button`, `link`, `heading`, `checkbox`, `radio`, `textbox`, `searchbox`, `combobox`, `listbox`, `option`, 
`img`, `list`, `listitem`, `table`, `row`, `cell`, `navigation`, `dialog`, `form` и ещё десяток.

А доступное имя элемента (`accessibleName`) вычисляется в правильном порядке: сначала `aria-labelledby`, 
потом `aria-label`, потом связанный `<label>`, потом `alt`, потом текст внутри, и в конце — `title`. 
Прямо как у судьи: сначала смотрим на VAR, потом на ассистента, а если совсем никто не помог — решаем на глаз.

Это ещё не полная реализация W3C AccName-алгоритма (рекурсивный обход поддерева и скрытые элементы пока не 
завезли), но для 95% реальных страниц этого за глаза хватит.

См. [issue 3359](https://github.com/selenide/selenide/issues/3359) и [PR 3336](https://github.com/selenide/selenide/pull/3336).

<br>

### Ещё MCP-тулзы для вашего ИИ-раба {#more-mcp-tools}

Помните, в прошлом релизе я сдался и завёз [Selenide MCP server](/2026/04/10/selenide-7.16.0/#selenide-mcp-server)? 
Ну так вот, аппетит приходит во время еды. ИИ-раб оказался мало на что способен без нормального инструментария, 
поэтому мы навайбкодили ему ещё девять тулзов:

* управление вкладками — `browser_tab_list`, `browser_tab_select`, `browser_tab_new`, `browser_tab_close`
* `browser_resize` — ресайз окна браузера
* `browser_fill_form` — заполнить сразу пачку полей одним вызовом
* `browser_wait_for` — подождать текст, исчезновение текста, состояние элемента или просто время
* `browser_network_requests` / `browser_network_request` — посмотреть, что там браузер вообще у бекенда просил 
  (требует `--proxy-enabled` при старте)

Теперь ваш ИИ почти как заправский нападающий: видит все вкладки на поле, умеет резаться в сетевые запросы 
и даже заполнять формы одним ударом, не тыкая в каждое поле по отдельности.

```
> claude
> /mcp enable selenide
> Раб, открой вкладку с турнирной таблицей, ресайзни окно под мобилку 
  и проверь, что запрос к /api/matches вернул 200
```

И он это сделает. Куда он денется.

См. [PR 3335](https://github.com/selenide/selenide/pull/3335).

<br>

### Скачивание нескольких файлов разом {#download-multiple-files}

Год назад [заказали](https://github.com/selenide/selenide/issues/3261) метод для скачивания нескольких файлов 
одним вызовом. С первого захода не взлетело, со второго — получилось:

```java
List<File> reports = $("#multiple-downloads").downloadFiles(files(5).withExtension("zip"));
assertThat(reports).hasSizeGreaterThanOrEqualTo(5);
```

Удобно, если один клик в интерфейсе триггерит скачивание сразу пачки файлов (отчётов, архивов, чего угодно), 
а вам надо проверить, что все они долетели и нужного формата.

См. [issue 3261](https://github.com/selenide/selenide/issues/3261) и [PR 3334](https://github.com/selenide/selenide/pull/3334).

<br>

### SelenideAppiumElement теперь честно возвращает SelenideAppiumElement {#appium-element-return-type}

Старая заноза, которой скоро already два года: если вызвать `find()`/`$()`/`$x()` у `SelenideAppiumElement`, 
он раньше подсовывал вам обычный `SelenideElement` — и все специфичные мобильные методы становились недоступны. 
Как будто нападающего вызвали в сборную, а на поле выпустили полузащитника.

Теперь `find()`, `$()`, `$x()`, `findAll()`, `$$()`, `$$x()` у `SelenideAppiumElement` возвращают именно 
Appium-элементы — по всей цепочке вызовов, а не только на первом уровне.

Спасибо [Stanislav Vasenkov](https://github.com/stanislavvasenkov) за то, что начал разгребать эту тему в 
[PR 3362](https://github.com/selenide/selenide/pull/3362), и за терпение, пока мы с ИИ-рабом докручивали 
это дело до полного решения.

См. [issue 2761](https://github.com/selenide/selenide/issues/2761) и [PR 3373](https://github.com/selenide/selenide/pull/3373).

<br>

### `Selenide.cookies()` — новый API для печенек {#cookies-api}

У Selenide давно есть красивый симметричный API для хранилищ:
```java
sessionStorage().clear();
localStorage().clear();
```

А вот для печенек такой красоты не было — только старые корявые `Selenide.clearBrowserCookies()` и проверки 
через `webdriver().shouldHave(cookie("name"))`. Непорядок исправили — теперь куки живут в той же линейке:

```java
cookies().clear();
```

Заодно объявили deprecated старые методы `clearBrowserLocalStorage()` и `clearBrowserCookies()` — 
пользуйтесь `localStorage().clear()`, `sessionStorage().clear()` и `cookies().clear()`.

Спасибо [Stanislav Vasenkov](https://github.com/stanislavvasenkov) за оба PR — и за новый API, и за то, 
что навёл порядок со старыми методами.

См. [PR 3375](https://github.com/selenide/selenide/pull/3375), а также 
[issue 3357](https://github.com/selenide/selenide/issues/3357) и [PR 3361](https://github.com/selenide/selenide/pull/3361).

<br>

### `element()`/`elements()` для котлин-разработчиков в Appium {#element-aliases-kotlin}

Если вы пишете Appium-тесты на Kotlin, то наверняка уже прокляли метод `` `$` ``  — в Kotlin его приходится 
экранировать бэктиками, и выглядит это как заклинание, а не как код:

```kotlin
// было
private val button = SelenideAppium.`$`(CombinedBy.android(...).ios(...))

// стало
private val button = SelenideAppium.element(CombinedBy.android(...).ios(...))
```

Добавили алиасы `element()`/`elements()` — точь-в-точь как в веб-версии `Selenide`. Никакой новой магии, 
просто человеческие имена методов вместо ритуальных плясок с бэктиками.

Спасибо [Gabriel Baldez](https://github.com/gabrielbaldez) за PR!

См. [issue 3348](https://github.com/selenide/selenide/issues/3348) и [PR 3350](https://github.com/selenide/selenide/pull/3350).

<br>

### Обновили Selenium с 4.44.0 на 4.45.0 {#update-to-selenium-4.45.0}
И подняли версию CDP с v148 до v149.

[Ченджлог](https://github.com/SeleniumHQ/selenium/blob/trunk/java/CHANGELOG)

См. [PR 3347](https://github.com/selenide/selenide/pull/3347).

<br>

### Обновили Selenium с 4.45.0 на 4.46.0 {#update-to-selenium-4.46.0}
И подняли версию CDP с v149 до v150.

[Ченджлог](https://github.com/SeleniumHQ/selenium/blob/trunk/java/CHANGELOG)

См. [PR 3374](https://github.com/selenide/selenide/pull/3374).

<br>

> Голкипер отбивает. Защитник выносит. `byRole("button")` кликает.  
> Роли у каждого свои, но результат нужен один — победа.

<br>
<br>


[Андрей Солнцев](http://asolntsev.github.io/)

ru.selenide.org